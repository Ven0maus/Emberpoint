namespace Emberpoint.Core.GameObjects.Conversation
{
    /* NOTE:
     * Enum names are formatted as such: XXX_Name
     * So FirstRoom = 1, will be formatted as: 001_FirstRoom
     * This format resembles the filename within the resources.
     */
    public enum Dialogues
    {
        FirstRoom = 1,
        FirstDoor = 2
    }
}
